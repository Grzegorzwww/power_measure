#include "delay.h"volatile u32 tick = 0;static volatile int tick_actual = 0;static volatile int tick_time = 0;//void SysTick_Handler(void) {//    tick++;//}inline void delay(uint32_t time, uint32_t load) {    tick = 0;    SysTick_Config(load);    do {        while (!(SysTick->CTRL && SysTick_CTRL_COUNTFLAG_Msk));    } while (tick < time);    SysTick->CTRL = 0;}void vIncrementDelayTicks(){	tick_actual++;}void delay_ms(uint32_t time) {//	tick_time = time *77;	tick_time = time * 77;	long int i;	for(i = 0; i < tick_time; ){		i++;	}	tick_time = 0;//	tick_time = time;//	while(tick_actual < tick_time){//		;//	}//	tick_actual = 0;//	tick_time = 0;}//inline void delay_ms(uint32_t time) {//    delay(time, SystemCoreClock / 1000);//}//inline void delay_us(uint32_t time) {//    delay(time, SystemCoreClock / 1000000);//}